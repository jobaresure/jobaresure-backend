package com.jobaresure.enums;

/** Government / business documents accepted for verification (India-focused). */
public enum DocumentType {
    // Identity (job seekers)
    AADHAAR,
    PAN,
    DRIVING_LICENSE,
    VOTER_ID,
    PASSPORT,
    // Company
    GST_CERTIFICATE,
    INCORPORATION_CERTIFICATE,
    BUSINESS_PAN,
    UDYAM_CERTIFICATE,
    OTHER
}
